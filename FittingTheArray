bool isFit(int a[], int b[], int n){
    // Complete the function
     sort(a, a + n);
     sort(b, b + n);
 
    for (int i = 0; i <n; i++)
        if (a[i] > b[i])
            return false;
 
    return true;
}