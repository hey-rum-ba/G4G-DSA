class Solution
{
    public:
    int count(struct node* head, int search_for)
    {
    //add your code here
    node* current = head;
    int count = 0;
    while (current != NULL) {
        if (current->data == search_for)
            count++;
        current = current->next;
    }
    return count;
    }
};