vector<int> inversePermutation(int arr[], int size) {
    vector<int> ans;
     for (int i = 0; i < size; i++) {
 
    for (int j = 0; j < size; j++) {

      if (arr[j] == i + 1) {
        ans.push_back(j + 1);
        break;
      }
    }
  }
  return ans;
}